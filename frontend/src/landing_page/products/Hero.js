import React from "react";

function Hero() {
  return (
    <section className="products-hero">
      <div className="container">
        <span className="section-label">Features</span>
        <h1 className="mt-3">Everything in one portfolio dashboard</h1>
        <p className="mt-3">
          Vestora brings together the core tools you need to track, analyze,
          and understand your investments — all in a single clean interface.
        </p>
      </div>
    </section>
  );
}

export default Hero;
